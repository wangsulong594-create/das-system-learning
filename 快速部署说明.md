# DAS系统学习项目 - 快速部署说明

## 🚀 一键部署（3种方式）

### 方式1：Python脚本（推荐，跨平台）

#### Windows用户
```bash
# 1. 确保已安装Python 3.6+
python --version

# 2. 运行部署脚本
python deploy.py

# 3. 按照提示输入GitHub Token
```

#### Mac/Linux用户
```bash
# 1. 确保已安装Python 3
python3 --version

# 2. 运行部署脚本
python3 deploy.py

# 3. 按照提示输入GitHub Token
```

---

### 方式2：Bash脚本（Linux/Mac）

```bash
# 1. 给脚本执行权限
chmod +x deploy.sh

# 2. 运行脚本
./deploy.sh

# 3. 按照提示输入信息
```

---

### 方式3：批处理脚本（Windows）

```cmd
# 在Windows命令行中双击或运行：
deploy.bat

# 按照提示输入GitHub Token
```

---

## 📋 前置条件

### 必须安装
1. **Git**
   - Windows：https://git-scm.com/download/win
   - Mac：`brew install git`
   - Linux：`sudo apt-get install git`

2. **GitHub账号**
   - 注册地址：https://github.com

3. **Personal Access Token**
   - 访问：https://github.com/settings/tokens
   - 点击 "Generate new token"
   - 权限选择 `repo`
   - 复制Token（只显示一次！）

### 可选安装
- Python 3.6+（用于Python脚本部署）

---

## 🔑 获取GitHub Personal Access Token

1. 登录 GitHub
2. 右上角头像 → Settings
3. 左侧菜单 → Developer settings
4. Personal access tokens → Tokens (classic)
5. Generate new token
6. 设置 Token 名称（如 "das-deployment"）
7. 在 scopes 中勾选 `repo`（全选）
8. 点击 "Generate token"
9. **立即复制 Token**（刷新后看不到！）

---

## ⚠️ 注意事项

### Token安全
- ❌ **不要**分享你的Token
- ❌ **不要**提交Token到仓库
- ✅ **要**定期轮换Token
- ✅ **要**在Token泄露时立即删除

### 脚本使用
- 脚本会自动创建所有必要文件
- 脚本会自动初始化Git仓库
- 脚本会自动推送到GitHub
- 脚本中的Token**不会被保存**（仅用于推送）

---

## 🆘 常见问题

### Q1: "git is not recognized"
**A**: Git未安装或未加入PATH
- 重新安装Git，勾选"Add Git to PATH"
- 或者手动添加Git到环境变量

### Q2: "Token authentication failed"
**A**: Token可能错误或已过期
- 确认Token已复制完整
- 确认未包含前后空格
- 重新生成新Token

### Q3: "repository already exists"
**A**: GitHub上已有同名仓库
- 手动删除GitHub上的仓库，再运行脚本
- 或修改脚本中的 REPO_NAME

### Q4: 脚本卡住不动
**A**: 可能在等待输入或网络问题
- 检查是否有输入提示（按Enter或输入Token）
- 检查网络连接
- 在终端中按 Ctrl+C 中断，重新运行

### Q5: 中文显示乱码（Windows）
**A**: 字符编码问题
- 脚本已设置 UTF-8 编码
- 如仍有问题，手动将编码改为 UTF-8

---

## 验证部署成功

### 访问仓库
```
https://github.com/wangsulong594-create/das-system-learning
```

### 检查内容
- [ ] 能看到 README.md
- [ ] 能看到 LICENSE 文件
- [ ] 能看到 .gitignore 文件
- [ ] 提交历史显示正确信息

---

## 部署后的下一步

### 1. 添加项目描述（可选但推荐）
1. 进入仓库主页
2. 右上角 "⚙️ Settings"
3. 找到 "Description" 和 "Topics"
4. 添加：
   - Description: `DAS系统完整学习记录 - 硬件、信号处理、系统集成`
   - Topics: `das`, `learning`, `signal-processing`, `optics`

### 2. 启用 Discussions（可选）
1. Settings → Features
2. 勾选 "Discussions"
3. 允许用户讨论和问答

### 3. 启用 Wiki（可选）
1. Settings → Features
2. 勾选 "Wiki"
3. 用于补充说明和文档

### 4. 邀请协作者（可选）
1. Settings → Collaborators
2. 添加协作者GitHub用户名

---

## 手动部署（如果脚本失败）

```bash
# 1. 创建目录
mkdir das-system-learning
cd das-system-learning

# 2. 初始化Git
git init
git config user.name "wangsulong594-create"
git config user.email "your-email@example.com"

# 3. 创建README
echo "# DAS系统学习记录" > README.md

# 4. 创建LICENSE
echo "MIT License" > LICENSE

# 5. 添加文件
git add .

# 6. 提交
git commit -m "Initial commit: DAS系统学习记录 v1.0.0"

# 7. 添加远程（用你的Token替换YOUR_TOKEN）
git remote add origin https://YOUR_TOKEN@github.com/wangsulong594-create/das-system-learning.git

# 8. 推送
git branch -M main
git push -u origin main
```

---

## 获取帮助

- **Git官方文档**：https://git-scm.com/doc
- **GitHub官方教程**：https://github.com/skills
- **GitHub API文档**：https://docs.github.com/en/rest

---

## 成功示例

```
$ python deploy.py
🚀 DAS系统学习项目 - GitHub自动部署工具

📋 步骤1：检查前置条件
✅ Git已安装

📁 步骤2：创建项目目录
✅ 目录已创建：das-system-learning

📄 步骤3：生成项目文件
✅ README.md已创建
✅ LICENSE已创建
✅ .gitignore已创建

🔧 步骤4：初始化Git仓库
✅ Git仓库已初始化

💾 步骤5：添加文件并提交
✅ 提交已创建

🔑 步骤6：输入GitHub Personal Access Token
请输入你的GitHub Token（不会显示）: ••••••••••••••••

📤 步骤7：推送到GitHub

🎉 恭喜！你的项目已经上传到GitHub！

仓库地址：https://github.com/wangsulong594-create/das-system-learning

✨ 部署完成！
```

---

**祝部署顺利！** 🚀

如有问题，欢迎反馈。
